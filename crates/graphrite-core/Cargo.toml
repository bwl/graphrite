[package]
name = "graphrite-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
nom = { version = "7", default-features = false, features = ["alloc"] }
regex = "1.10"
